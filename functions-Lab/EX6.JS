function sinCheck(numOne, numTwo, numThree) {
    let result = numOne * numTwo * numThree;

    if (result < 0) {
        console.log("Negative");
    } else {
        console.log("Positive");
    }
}
sinCheck(5, 12, -15)
sinCheck(-6, -12, 14);
sinCheck(-1, -2, -3);
sinCheck(-1, -2, -3);
